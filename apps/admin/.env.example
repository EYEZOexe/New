# Convex deployment URL (browser-safe).
NEXT_PUBLIC_CONVEX_URL=__SET_IN_COOLIFY__

# Comma-separated list of admin emails allowed to access the admin app.
NEXT_PUBLIC_ADMIN_EMAIL_ALLOWLIST=admin@example.com

