{
  "name": "g3netic-crypto-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build --concurrency=1",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "prettier -w .",
    "appwrite:bootstrap": "node scripts/appwrite/bootstrap.mjs",
    "appwrite:deploy:sellapp-webhook": "node scripts/appwrite/deploy-sellapp-webhook.mjs --env-file .env.appwrite"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  }
}
